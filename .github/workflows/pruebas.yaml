name: "Angular pp"

on:
  push:
    branches: [ "pruebas" ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run single service test
        run: npm test -- --watch=false --browsers=ChromeHeadless --include=**/categorias.spec.ts


